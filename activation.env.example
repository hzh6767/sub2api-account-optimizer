# Keep all gates false until the patched endpoint and dry-run output are verified.
OPTIMIZER_APPLY_ENABLED=false
OPTIMIZER_ACTIVE_PROBES_ENABLED=false
OPTIMIZER_TARGETED_TEST_SAFE=false

# Deployment-specific values.
OPTIMIZER_GROUP_IDS=7,59
OPTIMIZER_ADMIN_API_KEY_SOURCE=./secrets/admin-api-key
SUB2API_DOCKER_NETWORK=sub2api_sub2api-network
SUB2API_MODEL_PRICING_SOURCE=../data/model_pricing.json
